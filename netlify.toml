[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Set these values in Netlify UI instead of committing secrets.
  VITE_SUPABASE_URL = ""
  VITE_SUPABASE_ANON_KEY = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
